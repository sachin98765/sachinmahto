{
  "name": "mern-portfolio",
  "version": "1.0.0",
  "description": "Full-Stack MERN Portfolio Website",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c cyan,yellow \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
